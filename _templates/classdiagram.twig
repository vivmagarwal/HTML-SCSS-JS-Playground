ClassDiagram [frame=true framecolor=steelblue label="{{ title|replace({'-': ' '})|title }}"] {
 
/*
class Hero { private userId : string; public login(email: string, password:string) }
class HeroOne extends Hero {}
interface ISuperPowers { attack(t:type):returntype }
class fly implements ISuperPowers { attack(t:type):returntype }
align {
  Hero
  fly
}

Navigability & Associations:
a -- b  | a --> b | a --x b | a <--> b  | a <--x b | a x--x b 
classA -g-> classB : generalization association between two classes
classA -r-> interfaceB : realization of an interface : classA -r-> interfaceB
classA -a-> classB : aggregation
classA -c-> classB : composition
*/
  
}